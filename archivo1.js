/* VARIABLES */

const constante = 10; // Variable cuyo valor no cambia (no se puede sobrescribir)
var texto = '10'; /* <- variable que contiene un string */
var numero = 5; /* <- variable que contiene un tipo numero */
numero = numero + 1; // el valor de la variable se incrementa en 1 (haro vale 6)
numero++; // El valor se incrementa igual que el anterior
numero--; // el valor se decrementa en 1
alert(texto + numero); /* <- el '+' se utiliza para concatenar */
console.log